# Core data
yfinance>=0.2.40
financedatabase>=2.1.1
pandas>=2.0
numpy>=1.24

# Networking: caching, retries/backoff
requests-cache>=1.1
tenacity>=8.2

# Config & templating
pyyaml>=6.0
jinja2>=3.1
python-dotenv>=1.0

# API backend (serves data/results/ to the frontend; reads the same files)
fastapi>=0.110
uvicorn[standard]>=0.29

# Optional LLM write-up engine (only needed if config write-up.engine == llm)
# anthropic>=0.40

# Tests
pytest>=7.4
