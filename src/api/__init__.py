"""FastAPI backend that serves the screener's saved results to a frontend.

Read-only over the same files the screener already writes to ``data/`` — no new
database. See ``src/api/main.py`` for the app and endpoints.
"""
